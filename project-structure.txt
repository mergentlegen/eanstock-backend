leanstock-backend/
├── src/                         # Express application source code
│   ├── app.js                   # App setup, middleware, routes, Swagger UI
│   ├── server.js                # HTTP startup and graceful shutdown
│   ├── config/                  # Environment, Prisma, and Redis clients
│   │   ├── database.js          # PrismaClient singleton
│   │   ├── env.js               # Zod environment validation
│   │   └── redis.js             # Redis connection lifecycle
│   ├── controllers/             # HTTP handlers
│   │   ├── adminController.js   # Admin-only audit log endpoint
│   │   ├── authController.js    # Register/login/refresh/logout/me
│   │   └── inventoryController.js # LeanStock endpoint handlers
│   ├── middleware/              # Cross-cutting Express middleware
│   │   ├── auth.js              # JWT verification and RBAC
│   │   ├── errorHandler.js      # Standard JSON error envelope
│   │   ├── rateLimit.js         # Redis token bucket for auth endpoints
│   │   └── validate.js          # Zod request validation
│   ├── routes/                  # Route definitions grouped by resource
│   │   ├── admin.routes.js      # /admin endpoints
│   │   ├── auth.routes.js       # /auth endpoints
│   │   └── inventory.routes.js  # Locations, products, inventory, jobs
│   ├── schemas/                 # Zod request schemas
│   │   ├── auth.schema.js       # Auth validation rules
│   │   └── inventory.schema.js  # LeanStock validation rules
│   ├── services/                # Business logic and integrations
│   │   ├── audit.service.js     # Append-only audit log writer
│   │   ├── auth.service.js      # Auth workflows
│   │   ├── inventory.service.js # Tenant catalog, transfer, decay logic
│   │   ├── lock.service.js      # Redis lock for transfer concurrency
│   │   └── token.service.js     # JWT and refresh token management
│   └── utils/                   # Shared helpers
│       ├── asyncHandler.js      # Async route wrapper
│       ├── errors.js            # ApiError helpers
│       └── slug.js              # Tenant slug helper
├── prisma/                      # Prisma ORM source of truth
│   ├── schema.prisma            # Models, enums, indexes, relations
│   └── migrations/              # Baseline PostgreSQL migration
├── tests/                       # Jest unit and integration tests
│   ├── auth.integration.test.js # Auth flow and RBAC tests
│   ├── deadStock.test.js        # Pure dead-stock formula tests
│   ├── inventory.integration.test.js # Atomic transfer test
│   └── setup-env.js             # Test environment defaults
├── .github/workflows/ci.yml     # Lint, test, Docker build pipeline
├── .env.example                 # Safe environment template
├── Dockerfile                   # Production API image
├── docker-compose.yml           # Local API + PostgreSQL + Redis
├── openapi.yaml                 # OpenAPI 3 API contract
├── package.json                 # Node dependencies and scripts
└── README.md                    # Setup and architecture notes
